__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
node_modules/
dist/
.vite/

# Scratch runs from the dashboard. The committed result is reference.json.
data/runs/latest.json
# Credential-free sweep; the committed one is the agent sweep.
data/runs/sensitivity_baselines.json
# Written by scripts/demo.py.
data/runs/demo.json

# Razorpay live probe: the captured payload IS committed (it is the proof).
# Nothing else from that flow should be.

# Staged at build time from data/runs/ by web/client/scripts/stage-artifacts.mjs.
# Committing them would mean two copies of a 2 MB artefact that must agree.
web/client/public/data/
