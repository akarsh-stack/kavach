__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
node_modules/
dist/
.vite/

# Scratch runs from the dashboard. The committed result is reference.json.
data/runs/latest.json

# Razorpay live probe: the captured payload IS committed (it is the proof).
# Nothing else from that flow should be.
